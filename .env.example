# User Database Configuration (Authentication, Logins, Passwords, Alumni Profiles)
DB_USER_HOST=localhost
DB_USER_NAME=your_user_db_name
DB_USER_USER=your_user_db_username
DB_USER_PASS=your_user_db_password

# Content Database Configuration (Projects, Inventory, Events, News, System Logs)
DB_CONTENT_HOST=localhost
DB_CONTENT_NAME=your_content_db_name
DB_CONTENT_USER=your_content_db_username
DB_CONTENT_PASS=your_content_db_password

# Invoice Database Configuration (Invoices, Payments, Financial Records)
# config.php reads DB_RECH_* directly; DB_INVOICE_* serve as automatic fallbacks.
# Set only the DB_RECH_* variables unless you need the DB_INVOICE_* alias separately.
DB_RECH_HOST=localhost
DB_RECH_PORT=3306
DB_RECH_NAME=your_invoice_db_name
DB_RECH_USER=your_invoice_db_username
DB_RECH_PASS=your_invoice_db_password

# SMTP Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@yourdomain.com
SMTP_PASS=your_smtp_password
SMTP_FROM=noreply@yourdomain.com
SMTP_FROM_EMAIL=noreply@yourdomain.com
SMTP_FROM_NAME="IBC Intranet"

# Application Settings
# For local development use: http://localhost/offer
# For production use: https://intra.business-consulting.de
BASE_URL=https://intra.business-consulting.de
ENVIRONMENT=production

# EasyVerein API Configuration
EASYVEREIN_API_TOKEN=your_easyverein_api_token

# Microsoft Entra ID (Azure AD) Configuration
# App registered at https://portal.azure.com -> Azure Active Directory -> App registrations
# App name: IBC-Intranet | Supported account types: Single tenant (only my organization)
AZURE_TENANT_ID=01310861-1145-4b8f-8f74-f4362a90b3f0
AZURE_CLIENT_ID=a911e088-0b5a-4515-8d89-f72b5a74ea16
AZURE_CLIENT_SECRET=your_azure_client_secret
# Must exactly match the Redirect URI configured in Azure Portal -> App registrations -> Authentication
# Pay attention to: https vs http, and no trailing slash at the end.
# For local development: http://localhost/offer/auth/callback.php
# For production: https://intra.business-consulting.de/auth/callback.php
AZURE_REDIRECT_URI=https://intra.business-consulting.de/auth/callback.php
